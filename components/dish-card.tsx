"use client";
import type { DishRecord } from "@/lib/dishes";
import { translateDish, useLanguage } from "@/lib/i18n";
export function DishCard({dish,index=0}:{dish:DishRecord;index?:number}){const{language}=useLanguage();const shown=translateDish(dish,language);return <a href={`/menu/${dish.id}`} className="dish-card"><div className="dish-image"><img src={dish.image} alt={shown.name} style={{objectPosition:`${68+(index%3)*8}% ${42+(index%2)*15}%`}}/></div><div className="dish-card-body"><span className="eyebrow">{shown.category}</span><div className="dish-title-row"><h3>{shown.name}</h3><strong>€{dish.price.toFixed(2)}</strong></div>{shown.tags.length>0&&<div className="dish-tags">{shown.tags.map(tag=><span key={tag}>{tag}</span>)}</div>}<p>{shown.description}</p><span className="text-link">{language==="it"?"Dettagli del piatto":"查看菜品详情"} <span aria-hidden="true">↗</span></span></div></a>}
