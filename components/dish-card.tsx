import type { DishRecord } from "@/lib/dishes";
export function DishCard({dish,index=0}:{dish:DishRecord;index?:number}){return <a href={`/menu/${dish.id}`} className="dish-card"><div className="dish-image"><img src={dish.image} alt={dish.name} style={{objectPosition:`${68+(index%3)*8}% ${42+(index%2)*15}%`}}/></div><div className="dish-card-body"><span className="eyebrow">{dish.category}</span><div className="dish-title-row"><h3>{dish.name}</h3><strong>€{dish.price.toFixed(2)}</strong></div>{dish.tags.length>0&&<div className="dish-tags">{dish.tags.map(tag=><span key={tag}>{tag}</span>)}</div>}<p>{dish.description}</p><span className="text-link">查看菜品详情 <span aria-hidden="true">↗</span></span></div></a>}
